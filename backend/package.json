{
  "name": "fleet-nav-backend",
  "version": "1.0.0",
  "description": "Fleet navigation backend with GraphHopper routing and hazard alerts",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo \"No build step required\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "uuid": "^13.0.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
